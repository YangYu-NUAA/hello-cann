# vLLM-Ascend 服务化部署

## 本节目标

启动 OpenAI 兼容接口，完成一次请求，并记录基础性能指标。

## 第一轮记录

- vLLM-Ascend 推荐版本。
- CANN 和 `torch_npu` 版本组合。
- Qwen 主线模型。
- 启动命令、请求命令和指标口径。

本节不阻塞第一轮实验。先完成 Transformers baseline，再决定 vLLM-Ascend 是否进入第一版主线。
