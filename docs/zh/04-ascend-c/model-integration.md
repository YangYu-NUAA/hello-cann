# 接入模型链路

本页记录自定义算子如何接入 PyTorch / `torch_npu` 推理流程。

选择一个已经通过独立正确性测试的算子接入模型，并比较接入前后的结果和性能。

接入前先准备三份记录：

- PyTorch 参考实现的输入、输出和误差阈值。
- CANN 算子的单独测试结果。
- 接入模型前后的延迟、吞吐和 profile 结果。

可以从 RMSNorm、Softmax 或 SiLU / SwiGLU 中选择一个算子。
